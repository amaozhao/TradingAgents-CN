/// <reference types="next" />
/// <reference types="next/image-types/global" />

// Stable project-owned Next.js type references.
// Next.js still generates next-env.d.ts locally during dev/build, but that file
// contains mode-specific .next route type imports and is intentionally ignored.
